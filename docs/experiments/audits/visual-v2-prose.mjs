/**
 * A focused second pass over the reviewed Korean editorial edition.
 * Pure transform: no file, network, manuscript, or Notion writes.
 * Numbers, tables, citations, captions, and scientific scope remain fixed.
 */
import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';

export const REVIEWED_EDITORIAL_SHA256 =
  '57045151de3b951e685a185b8e098922d5791decc19662551bdad963ec18924b';

const numbers = (value) => value.match(/\d+(?:[.,]\d+)*/g) ?? [];
const selectedLines = (value, pattern) => value.split('\n').filter((line) => pattern.test(line));

export function reviseProse(source) {
  assert.equal(typeof source, 'string', 'Editorial input must be text');
  assert.equal(
    createHash('sha256').update(source).digest('hex'),
    REVIEWED_EDITORIAL_SHA256,
    'Input differs from the reviewed editorial edition; review it before updating this pass',
  );
  const original = source.replace(/\r\n/g, '\n');
  let text = original;
  const edits = [];
  const replaceOnce = (id, before, after) => {
    assert.equal(text.split(before).length, 2, `Expected one prose anchor: ${id}`);
    assert.notEqual(before, after, `Unchanged prose edit: ${id}`);
    assert.deepEqual(numbers(after), numbers(before), `Numeric sequence changed in ${id}`);
    text = text.replace(before, after);
    edits.push({ id, before, after });
  };

  const headings = [
    [
      '## 2. 목표를 이해하는 것과 지출을 통제하는 것의 차이',
      '## 2. 선행연구: 목표 정렬과 실행 권한',
    ],
    ['## 3. 에이전트는 계획을 바꿀 수 있지만, 한도는 바꿀 수 없다', '## 3. 권한 경계와 위협 모델'],
    ['## 4. 다음 호출을 허용하기 전에, 누적 효과를 계산한다', '## 4. 누적 효과를 검사하는 구조'],
    ['### 4.1 사용자가 확인할 것은 계획이 아니라 경제적 조건이다', '### 4.1 경제 의도 계약'],
    ['### 4.4 응답이 없다는 이유로 예산을 다시 풀어서는 안 된다', '### 4.4 대기 거래의 예산 예약'],
    ['### 6.2 결과를 바꾼 것은 무엇이었나', '### 6.2 구성요소별 영향'],
    [
      '## 7. 메타마스크에 적용한다면 어디부터 바꿔야 할까',
      '## 7. 메타마스크 에이전트 지갑의 적용 지점',
    ],
    ['## 9. 실행 권한을 통제하는 지갑을 향해', '## 9. 재현성 기록과 후속 검증'],
  ];
  headings.forEach(([before, after], index) => replaceOnce(`heading-${index + 1}`, before, after));

  replaceOnce(
    'takeaway-mechanism',
    '- 지갑 에이전트가 무엇을 설명하는지와 실제로 얼마를 쓸 수 있는지는 다른 문제다. 인텐트록은 사용자가 확인한 한도를 기준으로 호출 전체의 자산 유출, 권한 노출과 최종 자산을 검사한다.',
    '- 인텐트록은 지갑 에이전트가 이미 허용받은 지출과 권한을 기록하고, 다음 호출이 사용자의 한도를 넘는지 검사한다. 작업이 끝나면 요청한 자산이 남았는지도 확인한다.',
  );
  replaceOnce(
    'opening',
    '에이전트가 거래를 나누어 실행할 때, 각 호출에 대한 승인은 전체 요청에 대한 보장이 될 수 있을까. 이 글은 인텐트록 연구 프로토타입으로 그 간극을 살펴본다. 핵심은 사용자가 확인한 경제 의도와 실제 호출의 누적 효과를 연결하는 데 있다. 메타마스크 에이전트 지갑을 실무 사례로 삼되, 공개 문서를 바탕으로 만든 가드 모드(Guard Mode) 정책 모형과 운영 제품은 구분한다.\n\n증거도 두 층으로 나눴다. 이더리움(Ethereum)과 베이스(Base)의 고정 포크에서 기본 시나리오 80개의 실행과 참조 일치를 확인했고, 별도로 400개 실행 기록에서 다섯 정책을 비교했다. 전자는 선택된 완료 실행의 최종 목표 통과와 참조 불일치 0건에 관한 증거이며, 후자는 작성된 정책 위반을 허용하는지에 관한 비교다. 실제 자금 손실률은 측정하지 않았다. 이 구분 위에서 무엇이 개선됐고, 무엇이 여전히 해결되지 않았는지 살펴본다.',
    '지갑 에이전트는 한 요청을 여러 거래로 나누어 실행한다. 거래마다 대상과 금액을 확인하더라도 합산 지출이나 마지막에 남는 자산까지 관리하려면 앞선 실행의 기록이 필요하다. 인텐트록은 사용자가 확인한 경제 조건에 이 기록을 대조하는 연구 프로토타입이다. 메타마스크 에이전트 지갑의 공개 문서를 실무 사례로 사용했으며, 비교에 쓰인 가드 모드(Guard Mode)는 운영 제품을 재현한 것이 아닌 정책 모형이다.\n\n평가에는 두 종류의 자료를 사용했다. 이더리움(Ethereum)과 베이스(Base)의 고정 포크에서는 기본 시나리오 80개의 실행을 검증했다. 별도의 400개 실행 기록에는 다섯 정책을 적용해 작성된 위반의 허용 여부를 비교했다. 고정 포크에서 선택된 완료 실행은 모두 최종 목표를 통과했고 참조 불일치는 0건이었다. 정책 비교의 결과는 실제 자금 손실률과 구분해 제시한다.',
  );
  replaceOnce(
    'budget-example',
    '사용자가 지출 한도를 100 단위로 정했다. 에이전트는 60 단위를 쓰는 거래를 제안했고, 호출별 검사는 이를 허용했다. 다음에 제안한 거래도 60 단위라면 어떨까. 두 거래는 각각 한도보다 작지만, 합계 120 단위는 사용자가 허용한 범위를 넘는다. 이는 문제를 설명하기 위한 가상의 수치이지 실험 결과가 아니다.',
    '지출 한도가 100 단위인 요청을 예로 들자. 첫 거래에서 60 단위를 허용한 뒤 다음 거래의 60 단위를 원래 한도와만 비교하면, 합계 120 단위를 승인하게 된다. 두 번째 판단에는 첫 거래가 사용한 예산이 반영돼야 한다. 여기서 사용한 금액은 문제 설명을 위한 가상의 수치다.',
  );
  replaceOnce(
    'drift-causes',
    '이탈의 원인이 반드시 공격일 필요는 없다. 오염된 도구 결과가 수취인을 바꿀 수도 있고, 시간 초과를 거래 실패로 오인한 재시도(retry)가 지출을 중복시킬 수도 있다. 원인은 달라도 지켜야 할 자산 유출 한도는 같다. 이 글의 출발점은 여기에 있다. 에이전트의 설명이 그럴듯한지를 판단하는 일과 별도로, 서명할 실행 요청이 사용자가 확인한 경제적 범위를 지키는지 검사해야 한다.',
    '이러한 이탈은 공격과 단순 오류 모두에서 발생할 수 있다. 오염된 도구 결과가 수취인을 바꾸거나, 시간 초과를 거래 실패로 오인한 재시도(retry)가 지출을 중복시키는 경우다. 실행 직전의 검사는 이탈의 원인이나 에이전트의 설명보다 사용자가 허용한 수취인과 자산 유출 한도를 기준으로 작동해야 한다.',
  );
  replaceOnce(
    'contribution',
    '인텐트록이 처음 제안하는 것은 범용 의도 이탈 방지 장치(guardrail)가 아니다. 기여는 더 좁고 구체적이다. 지갑의 의도를 경제 조건으로 적고, 호출이 만드는 효과와 연결해 검사하는 프로토타입을 구현했다. 연구자가 작성한 실행 기록(trace)의 정책 비교와 고정 포크의 실행 증거를 분리해 보존한 것도 이 때문이다. 어디까지 확인했고 무엇이 아직 제안에 머무르는지 구분해야 설계 판단에 쓸 수 있다.',
    '본 연구의 기여는 경제 조건으로 표현한 지갑 의도를 호출의 누적 효과와 대조하는 프로토타입에 있다. 범용 의도 이탈 방지 장치(guardrail) 자체를 새로 제안하는 연구는 아니다. 연구자가 작성한 실행 기록(trace)의 정책 비교와 고정 포크의 실행 증거를 각각 보존해, 정책 판단에서 얻은 결과와 실제 실행으로 확인한 범위를 구분했다.',
  );
  replaceOnce(
    'contract-quality',
    '그렇다면 계약 자체가 사용자의 말을 잘못 옮기면 어떻게 될까. 의도 계약 생성·검사기(compiler)는 후보 계약의 형식, 필드별 근거가 신뢰하는 사용자 요청에 있는지, 구현된 권한 확대 규칙을 지켰는지를 검사한다. 그러나 근거 문자열이 있거나 형식이 맞는다고 사용자 의도가 빠짐없이 담겼다는 뜻은 아니다.',
    '계약을 만드는 단계에서도 누락이 생길 수 있다. 의도 계약 생성·검사기(compiler)는 후보 계약의 형식, 신뢰하는 사용자 요청에 있는 필드별 근거와 구현된 권한 확대 규칙을 검사한다. 이 검사는 형식과 근거 문자열을 확인하는 수준이며, 사용자 의도가 빠짐없이 표현됐는지까지 판정하지는 못한다.',
  );
  replaceOnce(
    'decoder-scope',
    '여기서 호출 해석의 범위를 구분할 필요가 있다. 주 오프라인 비교는 모든 바이트코드를 해석하는 실험이 아니다. 버전이 지정된 실행 기록에 연구자가 작성한 예측 경제 효과를 사용한다. 얕은 해석 설정에서도 중첩 효과를 지우지 않고 부분 해석(PARTIAL)으로 표시한다. 그 결과가 보여주는 것은 정보가 불완전할 때 보수적으로 중단하는 비용이지, 임의의 악성 호출에서 숨은 효과를 찾아내는 재현율이 아니다. 실제 거래 호출 데이터와 거래 영수증의 연결은 고정 포크 기본 사례와 별도의 서명 경계(signer-boundary) 사례에서 검사한다.',
    '주 오프라인 비교에는 버전이 지정된 실행 기록과 연구자가 작성한 예측 경제 효과를 사용했다. 얕은 해석 설정은 중첩 효과를 삭제하는 대신 부분 해석(PARTIAL)으로 표시한다. 따라서 이 비교로 측정할 수 있는 것은 불완전한 정보 때문에 중단하는 비용이다. 모든 바이트코드의 해석이나 임의의 악성 호출에서 숨은 효과를 찾아내는 재현율은 평가하지 않았다. 실제 거래 호출 데이터와 거래 영수증의 연결은 고정 포크 기본 사례와 별도의 서명 경계(signer-boundary) 사례에서 검사한다.',
  );
  replaceOnce(
    'monitor-transition',
    '매 호출의 판단에는 이전 판단의 결과가 들어간다. 실행 감시기는 확인된 계약, 이미 허용한 효과, 새 후보의 효과와 모의 실행·호출 해석 상태를 함께 받는다. 주 평가는 실행 순서대로 검사하고 허용(ALLOW)한 효과만 다음 판단에 누적한다. 범위나 상한을 확실히 넘으면 거부(DENY)한다. 관측이 불완전하거나 확인이 필요하면 사용자 확인을 요청한다(ESCALATE). 비교표는 이를 판단 유보(ABSTAIN)로 통일해 표시하며, 첫 ALLOW 아닌 판단에서 해당 기록의 자동 진행을 멈춘다.',
    '실행 감시기는 확인된 계약에 이미 허용한 효과와 새 후보의 효과를 합산하고, 모의 실행·호출 해석 상태를 확인한다. 주 평가에서는 실행 순서대로 검사해 허용(ALLOW)한 효과만 다음 판단에 누적했다. 범위나 상한의 명확한 초과는 거부(DENY)하고, 관측이 불완전하거나 확인이 필요한 경우에는 사용자 확인을 요청한다(ESCALATE). 비교표에서는 확인 요청을 판단 유보(ABSTAIN)로 표시한다. 해당 실행 기록의 자동 진행은 첫 ALLOW 아닌 판단에서 멈춘다.',
  );
  replaceOnce(
    'post-state-scope',
    '이 검사는 이상을 발견하는 장치이지 거래를 되돌리는 장치가 아니다. 지갑 연결 모듈은 불일치한 호출을 실행 후 불일치(EXECUTED_MISMATCH)로 반환하고 예약을 위반 상태(VIOLATED)로 남긴다. 실제 유출량은 후속 예산에 반영하지만, 이미 확정된 자산 이동을 취소하거나 계정의 모든 후속 요청을 일괄 동결하지는 않는다. 강제 동결과 사용자 복구 절차는 운영 배포 전에 따로 갖춰야 한다.',
    '불일치를 발견하면 지갑 연결 모듈은 실행 후 불일치(EXECUTED_MISMATCH)를 반환하고 예약을 위반 상태(VIOLATED)로 남긴다. 실제 유출량은 후속 예산에 반영한다. 여기까지가 현재 구현의 처리 범위다. 이미 확정된 자산 이동의 취소, 계정의 모든 후속 요청에 대한 일괄 동결과 사용자 복구 절차는 구현하지 않았으며 운영 배포 전에 별도로 다뤄야 한다.',
  );
  replaceOnce(
    'fork-evidence',
    '80/80이라는 숫자는 무엇을 확인한 결과인가. 고정 포크 검증은 기본 시나리오가 실행되고 작성된 참조와 맞는지를 확인한다. 각 시나리오의 첫 완료 시도를 선택하는 규칙으로 80/80개의 완료 증거를 확보했다. 최종 목표는 80/80개 통과(PASS), 참조 자료 대조는 80/80개이며 불일치는 0개였다. 그 전에 발생한 원격 노드 호출 실패 기록도 보존했다. 따라서 80/80을 첫 시도 성공률, 운영 가용성 또는 공격 방어 점수로 읽어서는 안 된다.',
    '고정 포크 검증의 80/80은 기본 시나리오의 실행 여부와 작성된 참조의 일치를 확인한 결과다. 각 시나리오의 첫 완료 시도를 선택해 80/80개의 완료 증거를 확보했다. 최종 목표는 80/80개 통과(PASS), 참조 자료 대조는 80/80개이며 불일치는 0개였다. 완료 전에 발생한 원격 노드 호출 실패 기록도 보존했으므로, 80/80은 첫 시도 성공률이나 운영 가용성, 공격 방어 점수를 나타내지 않는다.',
  );
  replaceOnce(
    'results-introduction',
    '가장 분명한 변화는 허용한 위반의 수였다. 인텐트록은 호출별 정책보다 적은 위반을 허용했지만, 정상 완료율은 높이지 못했다. 아래 표는 같은 사례 명세 목록에 대한 2,000개 시스템·사례 조합의 기록이다. 보안 수치만 낮추는 방법은 모든 요청을 멈추는 것일 수도 있다. 그래서 위반 허용, 정상 완료, 확인 부담과 비용을 함께 읽어야 한다.',
    '인텐트록은 호출별 정책보다 적은 위반을 허용했고 정상 완료율은 같았다. 아래 표는 같은 사례 명세 목록에 대한 2,000개 시스템·사례 조합의 기록이다. 요청을 중단하는 정책은 위반 허용과 완료를 모두 줄일 수 있으므로, 두 지표에 확인 부담과 비용을 함께 제시했다.',
  );
  replaceOnce(
    'unsafe-results',
    '작성 라벨을 기준으로 위반을 허용한 수는 방어 없음 240/400건, Guard Mode 정책 모형 93/400건, 대규모 언어 모델 판정기 54/400건, 호출별 정책 66/400건, IntentLock 10/400건이었다. 인텐트록에 남은 10건은 오래된 견적을 사용한 사후 상태 시험 자료에서 나왔다. 위반을 알 수 있는 정보가 실행 후에야 보였기 때문에, 그때 DENY를 반환해도 이미 허용한 효과는 위반으로 남는다. 사전에 관측 가능한 공격 230건에는 모두 개입했지만, 전체 공격은 240건이다. 두 분모를 바꾸어 쓰면 이 관측 한계가 사라져 보인다.',
    '작성 라벨 기준 위반 허용은 방어 없음 240/400건, Guard Mode 정책 모형 93/400건, 대규모 언어 모델 판정기 54/400건, 호출별 정책 66/400건, IntentLock 10/400건이었다. 인텐트록의 10건은 오래된 견적을 사용한 사후 상태 시험 자료에서 발생했다. 위반 판단에 필요한 정보가 실행 후에 관측됐고, 이후의 DENY로 이미 허용한 효과를 되돌릴 수는 없었다. 사전에 관측 가능한 공격 230건에는 모두 개입했다. 전체 공격 240건과의 차이는 이 관측 시점에서 발생한다.',
  );
  replaceOnce(
    'completion-denominator',
    '정상 완료율 50.00%도 그대로 사용자 경험에 대입할 수 없다. 분모 160건에는 정상 원본 80건뿐 아니라 비적대적 의도 이탈 80건이 들어 있다. 의도 이탈 집합은 해석할 수 없는 거래 호출 데이터 65건과 사후 불완전 상태 15건으로 구성돼 정상 원본과 조건이 다르다. 방어 없음·Guard Mode·호출별 정책·IntentLock의 80/160건 완료는 정상 원본 80건을 완료했다는 뜻이지, 자연스러운 사용자 업무의 절반이 실패한다는 추정이 아니다.',
    '정상 완료율 50.00%는 이 평가 집합의 구성과 함께 봐야 한다. 분모 160건은 정상 원본 80건과 비적대적 의도 이탈 80건으로 나뉜다. 후자에는 해석할 수 없는 거래 호출 데이터 65건과 사후 불완전 상태 15건이 포함된다. 방어 없음·Guard Mode·호출별 정책·IntentLock은 80/160건을 완료했으며, 완료된 사례는 정상 원본 80건이었다. 이 비율로 일반적인 사용자 업무의 실패율을 추정할 수는 없다.',
  );
  replaceOnce(
    'adaptive-scope',
    '이 평가는 질문부터 다르다. 정해진 공격 스크립트가 판단 사유를 보고 재계획할 때, 구현된 서명 경계를 넘는지를 모의 실행기로 확인했다. 실행 후 상태는 관측하지 않았다. 주 평가와 같은 사례를 짝지은 실험이 아니므로 비대응·비인과(NON_PAIRED_NON_CAUSAL) 비교다. 실제 공격 모델의 적응 능력, 포크 실행이나 운영 MetaMask를 시험한 결과도 아니다.',
    '별도의 모의 실행기에서 정해진 공격 스크립트가 판단 사유를 보고 재계획하도록 했다. 확인한 것은 구현된 서명 경계의 통과 여부이며 실행 후 상태는 관측하지 않았다. 주 평가와 사례가 대응되지 않는 비대응·비인과(NON_PAIRED_NON_CAUSAL) 비교로, 실제 공격 모델의 적응 능력이나 포크 실행, 운영 MetaMask에 대한 평가는 포함하지 않는다.',
  );
  replaceOnce(
    'negative-results',
    '인텐트록은 방어 없음·Guard Mode·호출별 정책보다 정상 완료를 높이지 못했고, 위반 허용 10건도 남겼다. 얕은 해석은 정상 작업 수행 능력만 낮췄고, 사후 상태 대조는 주 위반 허용률을 줄이지 못했다. 모든 정해진 공격 스크립트를 막은 집합에도 성공한 정상 서명기 실행은 없다. 낮은 위반 허용률과 별개로 남겨야 할 결과들이다.',
    '개선되지 않은 결과도 있다. 인텐트록의 정상 완료는 방어 없음·Guard Mode·호출별 정책과 같았고 위반 허용은 10건 남았다. 얕은 해석에서는 정상 작업 수행 능력만 낮아졌으며, 사후 상태 대조를 추가해도 주 위반 허용률은 줄지 않았다. 정해진 공격 스크립트를 모두 막은 서명 경계 평가에서도 정상 서명기 실행의 성공은 관측하지 않았다.',
  );
  replaceOnce(
    'limitation-opening',
    '가장 큰 한계는 감시기가 아니라 감시할 계약에서 시작한다. 복구 선호, 허용해서는 안 되는 사용 승인 대상이나 최종 자산이 계약에 빠지면, 계약을 정확히 지켜도 실제 의도에서 벗어날 수 있다. 이번 연구는 확인된 계약 이후의 실행을 다룬다. 사용자 연구, 자연어 의도 계약 생성·검사기의 독립 정확도, 모든 권한 확대를 의미적으로 포착하는 완전성은 입증하지 않았다.',
    '계약에 담기지 않은 의도는 실행 감시만으로 보호하기 어렵다. 복구 선호, 허용해서는 안 되는 사용 승인 대상이나 최종 자산이 빠진 계약을 그대로 지켜도 사용자가 원한 결과와 달라질 수 있다. 이번 연구의 범위는 확인된 계약 이후의 실행이다. 사용자 연구, 자연어 의도 계약 생성·검사기의 독립 정확도와 모든 권한 확대를 의미적으로 포착하는 완전성은 입증하지 않았다.',
  );
  replaceOnce(
    'closing',
    '이 연구에서 얻은 설계 판단은 지갑 에이전트의 안전을 모델의 설명에만 맡길 수 없다는 것이다. 허용된 도구라도 함께 실행하면 한도를 넘을 수 있다. 따라서 확인된 의도, 누적 경제 효과와 실제 서명 경로를 연결해야 한다. 인텐트록은 그 연결을 프로토타입과 범위가 고정된 비교로 보여주었지만, 사용자의 의미를 완전히 이해하거나 운영 지갑의 안전을 보장한 것은 아니다.\n\n다음 과제는 분명하다. 사용자가 자연어 계약을 제대로 확인할 수 있는지, 더 많은 프로토콜의 효과를 관측할 수 있는지, 여러 서명 서비스가 같은 예약과 복구 상태를 지킬 수 있는지를 검증해야 한다. 그때도 줄어든 위반만 볼 수는 없다. 정상 작업을 얼마나 완료하는지, 어떤 경우에 멈추는지를 각 지표의 분모와 관측 범위를 유지해 함께 평가해야 한다.',
    '이번 비교에서 실행 이력을 유지하는 정책은 호출별 정책보다 적은 위반을 허용했다. 반면 사후에야 드러난 위반은 막지 못했고, 정상 완료율도 높이지 못했다. 이 결과를 바탕으로 지갑 에이전트의 권한 관리에는 확인된 의도와 누적 효과를 실제 서명 경로까지 연결하는 설계가 필요하다고 본다. 인텐트록은 그 설계를 제한된 조건에서 시험한 프로토타입이며 사용자의 의미 이해나 운영 지갑의 안전을 보장하지 않는다.\n\n후속 검증에서는 사용자의 계약 확인 정확도, 추가 프로토콜의 효과 관측과 여러 서명 서비스 사이의 예약·복구 일관성을 다뤄야 한다. 정책이 멈춘 이유와 정상 완료에 미친 영향을 함께 기록하고, 각 지표의 분모와 관측 범위를 유지해 비교할 필요가 있다.',
  );

  assert.equal(edits.length, 25, 'Review the intended focused edit count');
  assert.deepEqual(numbers(text), numbers(original), 'Manuscript numeric sequence changed');
  assert.deepEqual(
    selectedLines(text, /^\|/),
    selectedLines(original, /^\|/),
    'Table rows changed',
  );
  assert.deepEqual(
    selectedLines(text, /^(?:Source:|!\[)/),
    selectedLines(original, /^(?:Source:|!\[)/),
    'Figure paths or Source captions changed',
  );
  assert.deepEqual(
    text.match(/https:\/\/[^)\s]+/g),
    original.match(/https:\/\/[^)\s]+/g),
    'Citation URLs or their order changed',
  );
  assert.equal(
    text.slice(text.indexOf('## 참고문헌')),
    original.slice(original.indexOf('## 참고문헌')),
    'References or appended review disclosure changed',
  );
  return { text, edits };
}
